server:
  port: 8080
spring:
  application:
    name: "pyp-medellin"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  data:
    mongodb:
      uri: "mongourl"
